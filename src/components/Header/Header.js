import React from 'react';

import { header } from 'components/Header/Header.module.scss';

const Header = () => {
    return(
        <header className={header}>Beer Recipe App</header>
    )
}

export default Header;